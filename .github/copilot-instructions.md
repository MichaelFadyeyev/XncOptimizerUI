<!--
  Modern GitHub Copilot reads AGENTS.md natively (nearest file in the tree
  wins), so this file intentionally stays a one-line pointer for older
  Copilot clients that don't yet look for AGENTS.md. Do not add real
  instructions here — add them to AGENTS.md at the repo root.
-->

See [AGENTS.md](../AGENTS.md) for full project instructions, including
where shared skills (`.agents/skills/`) and shared project-level agents
(`.agents/agents/`) live.
